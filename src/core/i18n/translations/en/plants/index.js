// English plant UI translations (if needed)
// Plant data itself comes from data files, not from translations

export const en = {
  // Only UI-specific translations here, if needed
  // Plant data is in src/core/db/plants/...
};
